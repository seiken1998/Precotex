export class Login {
    constructor(
        public user: string, // Inicializas el objeto Other - Opcional, ya que no lo necesitas
        public pass: string, // Inicializas el objeto Doors
    ){}
}